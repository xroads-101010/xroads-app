<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.xroads.dao.TripMemberLocationDao">

  <insert id="addMemberLocation" parameterType="TripMemberLocation" useGeneratedKeys='true' keyProperty='id' keyColumn="id">
    INSERT INTO
    `trip_member_location` (trip_member_id, current_location, created_at, updated_at)
    VALUES(
    #{tripMemberId},
    #{currentLocation}, #{createdAt}, #{updatedAt})
  </insert>
  
  <update id="updateMemberLocation" parameterType="TripMemberLocation">
    update `trip_member_location` set
    current_location = #{currentLocation},
    updated_at = #{updatedAt}
    where trip_member_id = #{tripMemberId}
  </update>

 

</mapper>